load("//rs:rules.bzl", "rust_binary")

package(default_visibility = ["//visibility:public"])

rust_binary(
    name = "airlock-loadout",
    srcs = glob(["*.rs"]),
    deps = [
        "//rs/ts",
        "@cargo//:esp-idf-sys",
    ],
)
