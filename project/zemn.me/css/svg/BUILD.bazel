load("//ts:rules.bzl", "ts_project")

package(default_visibility = [
	"//project/zemn.me:__subpackages__",
])

ts_project(
	name = "svg",
	assets = glob(["**/*.css"]),
	deps = [
        "//:base_defs",
	]
)
