load("//bzl:rules.bzl", "bazel_lint")
load("//ts:rules.bzl", "ts_project")

bazel_lint(name = "bazel_lint")

ts_project(
    name = "ensure_ids_unique_test",
    srcs = ["ensure_ids_unique_test.ts"],
    data = ["//project/zemn.me/bio:bio.ts"],
)
