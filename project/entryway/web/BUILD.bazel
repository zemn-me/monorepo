load("//bzl:rules.bzl", "bazel_lint")
load("//ts:rules.bzl", "ts_project")

ts_project(
    name = "web",
    deps = [
        "//project/entrway/api/client",
    ],
)

bazel_lint(name = "bazel_lint")
