# Cultist notes

- `BUILD.bazel` has hand-kept aggregate TS targets; preserve the `# keep` srcs/deps so Gazelle does not replace `index.ts` or drop the legacy React table.
- When present, the reference Cultist Simulator core content is in `.tmp/cultistsimulator-public/Assets/StreamingAssets/content/core`.
