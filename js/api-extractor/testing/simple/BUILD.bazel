load("//ts:rules.bzl", "ts_project")
load("//js/api-extractor:rules.bzl", "api_extractor")

api_extractor(
    name = "collate",
    srcs = [":project"],
    entry_point = "something.d.ts",
    publicTrimmedRollup = "rollup.d.ts",
)

ts_project(
    name = "project",
    srcs = ["something.ts"],
    deps = [ "//:node_modules/next"]
)
