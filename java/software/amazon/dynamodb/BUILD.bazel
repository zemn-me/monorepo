load("@rules_jvm_external//:defs.bzl", "artifact")

java_binary(
    name = "dynamodb_local",
    main_class = "software.amazon.dynamodb.services.local.main.ServerRunner",
    visibility = ["//visibility:public"],
    deps = [
        artifact("software.amazon.dynamodb:DynamoDBLocal"),
        artifact("software.amazon.awssdk:apache-client"),
    ],
)
