load("//ts:rules.bzl", "ts_project")

package(default_visibility = ["//:__subpackages__"])

ts_project(
    name = "canvas",
    srcs = glob([
        "*.ts",
        "*.tsx",
    ]),
    resolve_json_module = True,
    deps = [
        "//ts/math",
        "@npm//:@types/jest",
        "@npm//:@types/react",
        "@npm//:react",
    ],
)
