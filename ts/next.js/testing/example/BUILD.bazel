load("//bzl:rules.bzl", "bazel_lint")
load("//ts:rules.bzl", "ts_project")
load("//ts/next.js:rules.bzl", "next_project")

next_project(
    name = "example",
    srcs = [":ts_sources"],
)

bazel_lint(name = "bazel_lint")

ts_project(
    name = "launch_server_test",
    srcs = ["launch_server_test.ts"],
    deps = [
        "//:node_modules/@types/node",
        "//ts:guard",
    ],
)
