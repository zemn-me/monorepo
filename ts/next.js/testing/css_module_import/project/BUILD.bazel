load("//ts:rules.bzl", "ts_project")
load("//ts/next.js:rules.bzl", "next_project")
load("@npm//:next/package_json.bzl", "next")

next.next(name="next")

ts_project(
    name = "ts_sources",
    data = [":next"],
    deps = [
        "//ts/math",
        "//ts/next.js/testing/css_module_import/remote",
        "@npm//:@bazel/runfiles",
        "@npm//:@types/node",
        "@npm//:@types/react",
        "@npm//:react",
        "@npm//:react-dom",
    ],
)

next_project(
    name = "example",
    srcs = [":ts_sources"],
    distDir = "build",
)