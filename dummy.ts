/**
 * @fileoverview
 *
 * This is a dummy file that does nothing, needed
 * so that I can put .d.ts files in a ts_project --
 * so that in turn their proper ts deps are included.
 *
 * aspect_rules_ts does not allow ts_project rules
 * that only have deps and files -- they must produce
 * outputs.
 */
